
'use client';import{useEffect,useRef}from'react';import*as THREE from'three';import{EffectComposer}from'three/addons/postprocessing/EffectComposer.js';import{RenderPass}from'three/addons/postprocessing/RenderPass.js';import{UnrealBloomPass}from'three/addons/postprocessing/UnrealBloomPass.js';export default function P(){const r=useRef<HTMLDivElement>(null);useEffect(()=>{const s=new THREE.Scene();s.background=new THREE.Color(0);const c=new THREE.PerspectiveCamera(34,innerWidth/innerHeight,10.71,100);c.position.set(0,0,10.71);const e=new THREE.WebGLRenderer({antialias:true});e.setSize(innerWidth,innerHeight);e.setPixelRatio(Math.min(devicePixelRatio,1.5));e.toneMapping=THREE.ACESFilmicToneMapping;e.toneMappingExposure=1.8;r.current!.appendChild(e.domElement);const t=new EffectComposer(e);t.addPass(new RenderPass(s,c));const a=new UnrealBloomPass(new THREE.Vector2(innerWidth,innerHeight),1.5,.3,.15);t.addPass(a);s.add(new THREE.AmbientLight(0xffffff,2));const l=new THREE.PointLight(0xffffff,800,50);l.position.set(4,4,5);s.add(l);s.add(new THREE.PointLight(0xaaccff,500,50).add(new THREE.Vector3(-5,3,4)));const d=new THREE.PointLight(0xffffff,1000,20);s.add(d);const i=new THREE.Group();i.scale.setScalar(.84);s.add(i);const p=new THREE.Group();i.add(p);p.add(new THREE.Mesh(new THREE.IcosahedronGeometry(.62,3),new THREE.MeshBasicMaterial({color:0xffffff,wireframe:true,transparent:true,opacity:.55})));p.add(new THREE.Mesh(new THREE.IcosahedronGeometry(.78,2),new THREE.MeshBasicMaterial({color:0x88ccff,wireframe:true,transparent:true,opacity:.35})));p.add(new THREE.Mesh(new THREE.IcosahedronGeometry(.92,1),new THREE.MeshBasicMaterial({color:0xffffff,wireframe:true,transparent:true,opacity:.18})));const u=new THREE.MeshStandardMaterial({color:0xffffff,emissive:0xffffff,emissiveIntensity:8});const h=new THREE.Mesh(new THREE.IcosahedronGeometry(.48,5),u);i.add(h);const g=new THREE.Mesh(new THREE.IcosahedronGeometry(.22,3),new THREE.MeshBasicMaterial({color:0xffffff}));i.add(g);const m=new THREE.Mesh(new THREE.IcosahedronGeometry(.11,3),new THREE.MeshBasicMaterial({color:0xffffff}));i.add(m);const y=new THREE.Mesh(new THREE.SphereGeometry(.3,32,32),new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.25}));i.add(y);const b=new THREE.Group();i.add(b);for(let e=0;e<6;e++){const t=e*60*Math.PI/180;const a=new THREE.Mesh(new THREE.SphereGeometry(.05,16,16),new THREE.MeshBasicMaterial({color:0xffffff}));a.position.set(Math.cos(t)*.713,Math.sin(t)*.713,0);b.add(a);const o=new THREE.PointLight(0xffffff,200,3);o.position.copy(a.position);b.add(o)}let f=false;const v=()=>{if(f)return;f=true;a.strength=2.5;u.emissiveIntensity=12;g.scale.setScalar(1.8);m.scale.setScalar(1.5);y.scale.setScalar(1.5);d.intensity=1500;e.toneMappingExposure=2.2};setTimeout(v,300);addEventListener('pointerdown',v,{once:true});let w=0;const M=()=>{requestAnimationFrame(M);w+=.016;h.rotation.y+=.012;g.rotation.y-=.02;m.rotation.y+=.04;b.rotation.z+=.015;p.rotation.y+=.008;u.emissiveIntensity=8+Math.sin(w*3)*1.5+(f?4:0);y.scale.setScalar(1.2+Math.sin(w*2)*.15);t.render()};M()},[]);return<div style={{width:'100%',height:'100dvh',background:'#000'}}><div ref={r} style={{position:'fixed',inset:0}}/><div style={{position:'fixed',top:12,left:'50%',transform:'translateX(-50%)',background:'#3dd598',color:'#000',padding:'8px 20px',borderRadius:999,fontSize:12,fontWeight:900}}>⚡ IGNITION DIAMANT Z10.71</div></div>}
